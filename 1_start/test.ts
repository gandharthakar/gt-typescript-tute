let calc_plus = (a: number, b: number) => {
  return a + b;
};

let my_name = (fname: string, lname: string) => {
  return fname + " " + lname;
};

console.log(calc_plus(5, 5));

console.log(my_name("Hello", "World"));
